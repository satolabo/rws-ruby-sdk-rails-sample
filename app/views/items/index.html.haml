%h1 商品一覧
= form_tag(items_path, method: :get, class: 'form-inline') do
  .form-group
    %label{for: 'keyword'} キーワード
    = search_field_tag :keyword, @keyword, class: 'form-control'
  = submit_tag '検索', class: 'btn btn-default'

%table.table
  %thead
    %tr
      %th
      %th 商品名
      %th 価格
      %th 説明
  %tbody
    - @items.each do |item|
      %tr
        %td= image_tag item.medium_image_urls[0]['imageUrl']
        %td
          = item.name
        %td
          = item.price
        %td
          = item.caption
